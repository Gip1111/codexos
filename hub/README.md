# Aurion Hub

Aurion Hub is the alpha system overview surface.

In the final project this should become a Qt6/QML application. For the alpha ISO it generates a local HTML report from the existing AurionOS commands and opens it with the default browser. This gives a visible desktop-level entry point without adding package risk to the live image.
