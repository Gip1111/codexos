# AurionOS Rollback Foundation

The alpha rollback layer is read-only. It detects whether the current system is installed on Btrfs and whether tools such as Snapper or Timeshift are present.

The final target is a Btrfs snapshot strategy integrated with updates and recovery UI.
