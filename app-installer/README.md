# Aurion App Installation Foundation

AurionOS should eventually make app installation graphical and simple:

- Flatpak-first
- Snap optional during MVP
- trusted local `.deb` handling
- `.flatpakref` handling
- AppImage run-once and integrate flow

The alpha commands are intentionally conservative. They explain or prepare the action but do not silently install system packages.
