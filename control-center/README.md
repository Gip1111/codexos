# AurionOS Control Center Foundation

The alpha control center is a terminal-safe launcher that groups the OS features expected in the final product:

- system status
- hardware compatibility
- AI provider status
- app installation foundation
- diagnostics
- rollback status

The final implementation should become a Qt6/QML application. The alpha command is intentionally simple so it can ship in the live ISO without adding package risk.
